spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TeamC
    username: admin@10.3.1.226
    password: te4ateamc
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        '[hibernate.id.new_generator_mappings]': false
