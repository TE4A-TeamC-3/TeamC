spring:
  datasource:
    url: jdbc:mysql -u admin -p -h teamc-db.cmadu8gaa330.ap-northeast-1.rds.amazonaws.com
    username: springuser
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        '[hibernate.id.new_generator_mappings]': false