spring:
  datasource:
    url: jdbc:mysql://localhost:3306/teamkadai10db
    username: springuser
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        '[hibernate.id.new_generator_mappings]': false